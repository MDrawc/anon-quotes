<h2>Comments:</h2>
<% @article.comments.each do |comment| %>
  <% if comment.errors.any? %>
  <% else %>
    <p>
      <%= comment.commenter %>
      <em><%= comment.body %></em>
    </p>
    <% end %>
<% end %>
